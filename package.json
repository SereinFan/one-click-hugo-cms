{
  "name": "one-click-hugo-cms",
  "version": "1.0.0",
  "description": "Hugo template with Decap CMS ",
  "repository": "decaporg/one-click-hugo-cms",
  "scripts": {
    // ... 你的 scripts
    "build": "npm run build:webpack && npm run build:hugo", // 保持不变
    // ... 你的 scripts
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    // ... your devDependencies (保留不变)
  },
  "dependencies": {
    //移除 react 和 react-dom
    "date-fns": "^2.28.0",
    "decap-cms-app": "^3.0.2",
    "immutable": "^4.1.0",
    "netlify-plugin-cypress": "^2.2.1",
    "to-string-loader": "^1.2.0",

    // 添加 preact 和 netlify-cms-app
    "preact": "^10.11.2",
    "netlify-cms-app": "^2.15.69",

    // 添加 gray-matter (如果需要)
    "gray-matter": "^4.0.3" 
  }
}
